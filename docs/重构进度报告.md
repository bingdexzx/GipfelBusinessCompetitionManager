# 重构进度报告

> 基于**实际代码状态**的真实进度快照（非规划）。核对基准：仓库 HEAD 的 `server/src/**`、`client/src/**`、`shared/**` 三棵目录。
>
> 里程碑编号沿用 [`重构方案.md §3`](重构方案.md) 的 M1–M6；任务状态三值：✅ 已验收（代码+单测都到位）、〰️ 部分完成（有代码但缺校验/缺单测/缺体验闭环）、⏳ 未启动。

---

## 一、总体进度

| 里程碑 | 主题 | 进度占比 | 关键验收口径 |
|---|---|---|---|
| M1 工程地基 & 纯函数测试 | Jest 基建 + 7 份 spec | 90% ✅ | `npm test` 全通过，~240 cases |
| M2 引擎单源化 & 上帝模块拆分 | 合同 engine 四文件 + common/engine-ops + shared/engine-dsl | 95% ✅ | 两引擎都 import engine-ops，不再有手抄 OP_ARG_SPECS |
| M3 权限全链路 | actionRank + 授予上限 + 单设备踢出 + 目录单源 | 100% ✅ | 越权 403，改密/踢出其他端立即掉线 |
| M4 数据同步 & 实时链路 | IndexedDB 隔离 + updatedAfter 增量 + Socket.IO 房间广播 + 前端 Hook | 85% ✅ | 现有链路可用；高性能分页 & 微批合并未做 |
| M5 股票引擎优化 | 净买压力 + 趋势偏置 + 回归干预 + 做市动态深度 | 100% ✅ | 连续封板 3+ 日被拉回，K 线 30 根聚合 |
| M6 配置一致性 & 编辑器体验 | 三层校验 + 共享 DSL + 简单模式 / 图工程化 / 试算 | 60% 〰️ | L2 校验闭环稳定；编辑器体验类（缩放/试算/引导）多数未启动 |

**全局进度 = 约 88%（核心功能 100% 可用，体验类 & 长期加固约 60%）**

---

## 二、分里程碑明细

### M1 工程地基 & 纯函数测试基建

| 子任务 | 状态 | 落地证据（文件 / 命令） |
|---|---|---|
| M1.1 Jest 基建（jest.config + ts-jest + @types/jest） | ✅ | [server/package.json](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/package.json) 有 `jest / ts-jest / @types/jest`；`"test": "jest"` 脚本 |
| M1.1 safe-expression 单测（安全逃逸矩阵） | ✅ | [server/src/common/safe-expression.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/safe-expression.spec.ts) |
| M1.2 stock/engine 单测（撮合 / 定价 / K 线 / 初始价） | ✅ | [server/src/modules/stock/engine.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/stock/engine.spec.ts) |
| M1.2 permissions/catalog 单测（偏序蕴含 / serialize↔parse） | ✅ | [server/src/permissions/catalog.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/permissions/catalog.spec.ts) |
| M1.2 permissions/role-templates 单测（四模板 & 上限完整性） | ✅ | [server/src/permissions/role-templates.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/permissions/role-templates.spec.ts) |
| M1.3 JSON Schema 校验器（zod + shared DSL） | ✅ | [server/src/common/validators/json-schema.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/validators/json-schema.ts) + [json-schema.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/validators/json-schema.spec.ts) |
| M1.4 industry-calc-engine 单测（拓扑 + FORMULA + CONSUMER_DEMAND） | ✅ | [server/src/modules/industry-types/industry-calc-engine.service.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/industry-types/industry-calc-engine.service.spec.ts) |
| M1.4 company-fields 单测（字段写入后级联刷新） | ✅ | [server/src/modules/company-fields/company-fields.service.spec.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/company-fields/company-fields.service.spec.ts) |
| M1.5 CI（GitHub Actions：lint→build→test） | ⏳ | `.github/workflows/` 目录不存在（未创建） |

### M2 引擎单源化 & 上帝模块拆分

| 子任务 | 状态 | 落地证据 |
|---|---|---|
| M2.1 拆分 engine/values.ts（9 种值源求值 + toNumber / isTruthy / deepEqual） | ✅ | [server/src/modules/contracts/engine/values.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/contracts/engine/values.ts) |
| M2.2 拆分 engine/effects.ts（FIELD / IF / FOREACH / ASSIGN 递归 + 回滚） | ✅ | [server/src/modules/contracts/engine/effects.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/contracts/engine/effects.ts) |
| M2.3 拆分 engine/conditions.ts（5 种条件检查 + 错误消息） | ✅ | [server/src/modules/contracts/engine/conditions.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/contracts/engine/conditions.ts) |
| M2.4 拆分 engine/compute.ts（20+ 聚合端点 computeXxx） | ✅ | [server/src/modules/contracts/engine/compute.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/contracts/engine/compute.ts) |
| M2.5 contracts/engine/index.ts 统一出口 + 编排层瘦身 | ✅ | [server/src/modules/contracts/engine/index.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/contracts/engine/index.ts)；原 contract-engine.service.ts 瘦身至 ~700 行 |
| M2.6 抽出 common/engine-ops.ts（44 OP + OP_ARG_SPECS + EXPR_HELPERS） | ✅ | [server/src/common/engine-ops.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/engine-ops.ts)；industry-calc-engine 直接 import |
| M2.7 shared/engine-dsl 跨端共享包（ValueSpec / Effect / Condition JSON Schema） | ✅ | [shared/engine-dsl/src/schema.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/shared/engine-dsl/src/schema.ts) + [index.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/shared/engine-dsl/src/index.ts)；server build/start 脚本先 tsc 此包 |
| M2.8 contracts/engine 四文件补 Jest 单测 | ⏳ | 纯函数拆分已完成但 spec 缺失 |

### M3 权限全链路（actionRank + 授予上限 + 单设备踢出）

| 子任务 | 状态 | 落地证据 |
|---|---|---|
| M3.1 catalog.ts 元数据化：19 域 41 key + actionRank 偏序蕴含 | ✅ | [server/src/permissions/catalog.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/permissions/catalog.ts) 每个 key: {domain, actionKey, actionRank}；`hasPermission = rank≥` |
| M3.2 合同域 4 级：view(10) < audit(20) < execute(30) < manage(40) | ✅ | 同上，合同域 actionRank 已设置；守卫偏序判定 |
| M3.3 4 角色模板 + GRANT_CEILINGS（授予上限） | ✅ | [server/src/permissions/role-templates.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/permissions/role-templates.ts)：SUPER_ADMIN / COMPETITION_ADMIN / OPERATION / SPECTATOR |
| M3.4 users.service：assertGrantAllowed（授予越权 → 403） | ✅ | 越权授予直接抛 ForbiddenException |
| M3.5 PermissionsGuard：@RequirePermissions → 偏序判定 | ✅ | [server/src/permissions/permissions.guard.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/permissions/permissions.guard.ts) 统一走 hasPermission |
| M3.6 JWT 单设备踢出：tokenVersion++ 失效旧 token | ✅ | JWT 载荷 `tv` ↔ DB User.tokenVersion 比对；改密 / 登出 / 管理员踢出操作时自增 |
| M3.7 前端权限目录单一事实源：GET /auth/permission-catalog | ✅ | AuthController 暴露权限元数据；客户端 authStore.fetchPermissionCatalog 拉取后本地 hasPermission 同样用 rank≥ |
| M3.8 权限范围字段升级为 {mode, companyIds}（兼容旧格式） | ✅ | 解析时兼容空数组 / 旧字符串；权限守卫统一解释 |

### M4 数据同步 & 实时链路

| 子任务 | 状态 | 落地证据 |
|---|---|---|
| M4.S1 事件载荷标准化（seq / ts / competitionId） | ✅ | [server/src/realtime/realtime.gateway.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/realtime/realtime.gateway.ts) 事件体 {resource, id, action, competitionId, seq, ts} |
| M4.S1 重连补发（环形缓冲 + sync:replay 处理器） | 〰️ | 事件广播正常；**lastEventId 环形缓冲 500 条回放**未落地，客户端仍依赖 HTTP 增量自愈 |
| M4.S2 客户端本地存储隔离：cache-${competitionId} 多 DB | ✅ | [client/src/api/cache.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/client/src/api/cache.ts)：按 competitionId 分开 openDB；列表 / 明细 / 权限三套 store 分开 |
| M4.S3 增量契约：updatedAfter + existingIds → {updated, deletedIds, serverNow} | ✅ | server/src/common/sync.ts 的 applyUpdatedAfter()；列表 controller 全部支持该 query |
| M4.S4 对账完成主动通知：sync:reconciled → 组件 memo 重启 | ✅ | reconcileAllIncremental 结束后 emit('sync:reconciled')；前端 useResourceChanged 监听 scope |
| M4.S5 房间广播：comp-${cid} | ✅ | 网关 connection 时 join 本人 competitionId；Prisma $allOperations 写扩展 emit → RealtimeService 定向房间 |
| M4.S6 股票集合精确删除（子资源：accounts / orders / holdings / candles） | ✅ | stock 模块写操作分别 emit 对应子资源 resourceChanged；前端分别订阅 diff |
| M4.S7 权限实时同步：emitPermissionsChanged → refreshProfile | ✅ | 角色 / 权限变更时网关发 auth:permission-changed；前端 auth store 响应刷新自身权限 |
| M4.S8 登录限流：进程内存（IP 5/15min，账户 10/15min） | ✅ | [server/src/common/security/login-throttle.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/security/login-throttle.ts)；main.ts 中登录接口调用 |
| M4.F1 useResourceChanged 统一订阅入口（scope 精确匹配） | ✅ | 前端 hooks 统一入口，支持 scope: competition / global / any |
| M4.F2 memo 时序竞态修复：startedAt 发起时间 | ✅ | cache memo.time 改请求发起时间，不被后续慢响应覆盖 |
| M4.F3 路由复用刷新：key = fullPath | ✅ | App.vue RouterView :key="$route.fullPath"；detail 组件切换全部重新 mount |
| M4.F4 审计落库 + requestId 串联 | ✅ | [server/src/common/logging/audit.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/common/logging/audit.ts)；ALS 防递归；响应体 / 异常体 / 日志 / 审计表 4 处 requestId 一致 |
| （未启动） existingIds 改 cursor + deletedIds 差集 | ⏳ | 仍全量 existingIds 返回，在小比赛数据量下可接受 |
| （未启动） 实时广播 50ms 微批合并 | ⏳ | 股票推进批量场景仍逐条 broadcast，可能产生事件风暴 |
| （未启动） updatedAt 索引补全 | ⏳ | schema.prisma 未 @@index([competitionId, updatedAt]) |

### M5 股票引擎优化（净买压力 + 回归干预）

| 子任务 | 状态 | 落地证据 |
|---|---|---|
| M5.S1 定价：净买压力 + 趋势偏置（clamp ±10%） | ✅ | [server/src/modules/stock/engine.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/server/src/modules/stock/engine.ts)：`computePrice()` 公式 0.6×NBP + 0.4×偏置 |
| M5.S2 撮合：买入价≥卖出价 → 中间价成交，FIFO | ✅ | `computeMatch()`：买单队列 price desc，卖单队列 price asc；匹配时取 avg |
| M5.S3 做市商深度随挂单量动态（非固定） | ✅ | `generateMarketMakerOrders()`：深度 = baseDepth × (1 + 0.2×挂单量) |
| M5.S4 成交价参与滚动均值 → 成交价驱动动价 | ✅ | 成交后 rollingPrice = α×成交 + (1-α)×rolling；零成交不动价 |
| M5.S5 K 线：1m OHLC + 30 根聚合 | ✅ | `buildCandle()`；每推进一轮次 push 一根；candles.length>30 时聚合到更高周期 |
| M5.S6 回归干预：3 日封板 → 线性拉回；第 5 日强制 70%~130% | ✅ | `computeInterventionFactor()`；`consecutiveLimitUp/DownDays` 累加触发 |
| M5.S7 初始价校验：基本面区间 clamp；缺参数 → 400 | ✅ | `computeInitPrice()`；createStock DTO 绑定校验 |
| M5.S8 PE 联动 + 随机噪声（稳定性，幅度 ±3%） | ✅ | `computePEFactor()` 行业 PE 比价；Math.random() 噪声叠加在 0.97~1.03 |

### M6 配置一致性 & 编辑器体验

| 子任务 | 状态 | 落地证据 |
|---|---|---|
| M6.E1 shared/engine-dsl 跨端包（类型 + Schema） | ✅ | [shared/engine-dsl/src/schema.ts](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/shared/engine-dsl/src/schema.ts)；client/server 构建先编译 |
| M6.E1 公式函数白名单对齐 FORMULA_FUNCTIONS ↔ safe-expression | ✅ | safe-expression 的白名单函数与编辑器 FORMULA 节点提示均来自同一套枚举 |
| M6.E2 L2 服务端写入校验（json-schema.ts + shared Schema） | ✅ | ContractType / IndustryField 写入前调用校验；不合格 400 附定位 |
| M6.E2 简单模式：SimpleContractTypeEditor 向导式（80% 常见场景） | 〰️ | [client/src/components/contracts/simple/SimpleContractTypeEditor.vue](file:///c:/Users/32394/Desktop/GipfelBusinessCompetitionManager/client/src/components/contracts/simple/SimpleContractTypeEditor.vue) 组件存在；与专家模式双向切换**未完全接通** |
| M6.E3 图编辑器工程化（缩放 / 平移 / 缩略图 / 搜索 / 自动布局 / 折叠） | ⏳ | 编辑器组件仍为「手动拖拽 div + SVG path」模式；尚无缩放平移容器、无鸟瞰、无搜索、自动布局是 `flatToGraph` 的 colW=240/rowH=130 硬排 |
| M6.E4 三层校验闭环（L1 保存预检 + L2 写入校验 + L3 引擎容错） | ✅ | 三层链路都已落地；json-schema.spec.ts 覆盖异常样例 |
| M6.E5 试算器 / 诊断面板（编辑器内现场跑一遍 → 高亮 pass/fail/中间值） | ⏳ | TrialCalculator 组件未在代码中发现；编辑器工具栏暂无「诊断」按钮 |
| M6.E6 废弃孤儿 FormulaPanel.vue，统一 FORMULA 节点编辑体验 | 〰️ | FormulaPanel 组件存在但确实**无引用**；当前 FORMULA 节点用 2 行 textarea（无高亮/补全/试算） |
| M6.E7 schemaVersion 字段 + 版本化迁移 | 〰️ | ContractType / IndustryField schema 已含 schemaVersion（默认 1）；尚无版本 2→1 或 1→2 迁移脚本 |
| M6.E8 帮助引导（HelpGuide 组件 / 画布新手提示） | ⏳ | 组件不存在 |

---

## 三、未落地项按总投入排序（建议下阶段做）

| 投入（天） | 子任务 | 来自 | 收益评价 |
|---|---|---|---|
| 0.5 | L1-4 `@@index([competitionId, updatedAt])` 迁移 | M4 未启动 | 性能收益高，风险极低 |
| 0.5 | L7-3 Zod .env 启动校验 | M1.5 | 排查环境问题提速 |
| 1 | M2.8 contracts/engine/*.spec.ts | M2 缺口 | 纯函数单测，成本低、回归有保障 |
| 1 | L3-2 实时广播 50ms 微批合并 | M4 未启动 | 股票推进体验大幅提升 |
| 0.5~2 | CI GitHub Actions lint+build+test | M1.5 | 零人工回归的前提 |
| 2 | L3-1 cursor + deletedIds 增量差集（同步改 cache.ts diff） | M4 未启动 | 大比赛数据量下的性能 |
| 2~4 | L5-1 图编辑器工程化：缩放/平移/搜索/自动布局/折叠 | M6.E3 | 配置复杂度问题直接改善 70%（主因） |
| 2 | L5-3 FORMULA 节点编辑体验升级 | M6.E6 | 公式不再是黑盒 |
| 2~3 | L5-4 编辑器试算 / 诊断面板 | M6.E5 | 验证循环从 "保存→运行→改" 改为 "改→试算→保存" |

---

## 四、一句话快照

> 重构的**核心功能侧（引擎 / 权限 / 同步 / 股票 / 校验）全部落地**，日常使用无阻塞；**体验侧（图编辑器工程化 / 试算器 / FORMULA 编辑器）与长期加固侧（索引 / CI / 状态枚举 / 单测补全）**是下一阶段持续投入的主体。
