# 数据同步与实时刷新重构设计

> 版本：v2（决策已确认，含前端刷新专章）
> 状态：设计定稿，进入重构实施阶段
> 本文合并《数据同步与实时机制重构设计.md》（第一部分，S 系列任务）与《前端刷新机制重构设计.md》（第二部分，F 系列任务）。两文档强耦合：前端刷新是数据同步 §4.3 的深化专章，F 系列承接 S 系列；合并后消除互引冗余，单一事实源。
> 关联代码基线：`client/src/api/cache.ts`、`client/src/api/request.ts`、`client/src/realtime/resource-changed.ts`、`client/src/realtime/socket.ts`、`client/src/realtime/useResourceChanged.ts`、`client/src/stores/auth.ts`、`client/src/utils/accountStorage.ts`、`client/src/AppLayout.vue`、`client/src/composables/useCompetitionReload.ts`；`server/src/realtime/*`、`server/prisma/schema.prisma`、`server/src/common/guards/competition-scope.guard.ts`
> 阅读对象：本系统维护者。本文档覆盖「本地存储 / 增量同步 / 前端刷新 / 实时同步 / 数据比赛隔离」五件事的重构蓝图；与《权限分级与重构设计.md》互补（权限分级管"谁能做什么"，本文档管"数据如何到达界面"）。

---

# 第一部分 数据同步与实时机制（S 系列）

> 原《数据同步与实时机制重构设计.md》内容，任务编号 S1–S12。

## 1. 背景与目标

当前客户端已有「IndexedDB 全量副本 + updatedAt 增量基线 + WebSocket 实时事件」的雏形，但存在四类被反复反馈的痛点：

1. **本地存储与增量同步内容冲突**：写操作失效缓存与在途增量请求存在竞态，副本可能被旧数据覆盖回写；
2. **前端界面总是不刷新**：断线重连对账不通知组件、15 秒内存新鲜度窗口返回旧值、事件订阅漏配等；
3. **权限变更不同步**：管理员给某账户加权限后，对方界面菜单/按钮不更新（服务端 API 其实已即时生效）；
4. **数据未强制按比赛隔离**：除产业类型、合同类型、系统账户外，仍有 22 个模型的 `competitionId` 可空，可写入跨比赛全局数据。

本次重构目标：

1. **本地存储按「比赛 + 账户」双层隔离**，切换比赛/账户不串档、不残留；
2. **增量同步以服务端为唯一事实源**，消除覆盖冲突与基线回退；
3. **前端刷新机制重构**：事件驱动 + 比赛/资源精确匹配 + 对账完成主动通知；
4. **实时同步机制重设计**：事件携带 `competitionId` 与序号、断线补拉、定向推送；
5. **非数据类信息实时主动同步**（权限 / 消息 / 公告 / 比赛配置），权限变更即时生效，杜绝"我加了权限对方看不到"；
6. **除产业类型、合同类型、系统账户外，所有数据强制关联比赛**，加固多租户隔离。

---

## 2. 现状盘点（基线）

### 2.1 本地存储现状

| 层 | 实现 | 隔离维度 |
|----|------|---------|
| IndexedDB | 原生 API，库名 `gipfel-client-cache-u<id>`（`cache.ts:18-22`），objectStore `responses`，keyPath `key` | **按账户分库**；比赛靠集合键内 `competitionId=` 参数区分（`FULL|material|competitionId=1`） |
| 缓存键 | `FULL|<资源>|<非分页参数>`、`META|baseline|...`、`META|fullSyncAt|...`（`cache.ts:24-28`） | 22 类资源（`SEG_TO_RESOURCE`，`cache.ts:31-55`） |
| axios 缓存 | 两级：**O3 内存 memo**（15s 新鲜度窗口，`request.ts:157,555`）+ **IndexedDB 全量副本/增量**（`cachedGetImpl`，`request.ts:476-536`） | memo 键 = `GET url params`，**无账户/比赛维度**（`_reqKey`，`request.ts:180-183`） |
| localStorage | `utils/accountStorage.ts` 前缀 `acct_u<id>__`（`accountStorage.ts:45-47`） | 按账户；比赛维度仅 Dashboard 布局键（`DashboardView.vue:270-274`） |

### 2.2 增量同步现状

- 基线 = 服务端返回的最大 `updatedAt`（`maxUpdatedAtOf`）；增量请求带 `updatedAfter=<基线>`；`patchFullItems` 按 id 合并（`cache.ts:175-196`）；
- 删除复核：5 分钟周期（`FULL_SYNC_INTERVAL_MS`）或断线重连（`reconcileAllIncremental`，`request.ts:601-681`）时带 `requireExistingIds`，用 `existingIds` diff 删除；
- 写操作成功后 `invalidateResource` + `_resetMemo`（`request.ts:578-586`）；
- 离线降级：读本地全量副本（`request.ts:530-533`）。**无离线写队列**。

### 2.3 实时推送现状

- 网关 `server/src/realtime/realtime.gateway.ts`：握手 JWT 鉴权 → `join("user-{id}")`；客户端 `subscribe {competitionId}` → `comp-{cid}` 房间（非超管只能订阅自身比赛）；
- 事件清单（`server/src/realtime/realtime.service.ts`）：

| 事件 | 触发点 | 载荷 | 房间 |
|------|--------|------|------|
| `resource:changed` | Prisma `$allOperations` 中间件自动广播（`prisma.service.ts`），`MODEL_TO_RESOURCE` 25 模型 | `{resource, id, action}`——**不含 competitionId**（`realtime.service.ts:103`） | `comp-{cid}` 或全体（cid 为 null 时） |
| `message:new` | `message.service.ts` 定向收件人 | `{id,title,content,senderId,...}`——不含 competitionId | `user-{id}` |
| `company-field:changed` | company-fields / contract service | `{companyId, competitionId}` | `comp-{cid}` |
| `contract:changed` | contract.service | 合同对象 + competitionId | `comp-{cid}` |
| `competition:changed` / `fiscal-year:changed` | competition/files service | 实体 | 全体/房间 |
| `stock:round-advanced` | stock.service | 轮次信息 | `comp-{cid}` |

- **权限变更无定向推送**：`users.service.update()` 改角色/权限/范围后仅写库；Prisma 中间件会广播泛化 `resource:changed(users)`，但前端只把它当账号列表缓存失效（`AccountManagementView.vue:442` 刷新列表），**auth store 不消费** → 被改账户的菜单/按钮仍按旧权限渲染（`AppLayout.vue:40` 仅在挂载时拉一次 `/auth/me`）。服务端 API 层权限**即时生效**（`jwt.strategy.ts` 每次请求从 DB 重载 permissions/scopes），问题纯在前端本地状态。

### 2.4 数据与比赛关联现状（32 模型）

| 归属 | 模型 |
|------|------|
| 强关联（必填） | `Competition`（本体）、`FiscalYear`、`Contract` |
| 豁免（可全局） | `User`（系统账户）、`IndustryType` / `IndustryField`（产业类型）、`ContractType`（合同类型） |
| **弱关联（`competitionId Int?` 可空，不合要求）** | `Material` `Part` `Product` `TechNode` `MapNodeType` `PathType` `MapNode` `MapEdge` `Infrastructure` `Warehouse` `ProductionLine` `Fuel` `Vehicle` `Region` `ConsumerDemand` `Company`（16 个基础数据）+ `Stock` `StockFundsAccount` `StockHolding` `StockOrder` `StockCandle`（5 个股票）+ `Message`（`competitionId Int?` 可空，注释明示不靠它做租户隔离） |
| 间接关联 | `PartMaterial` `PartTechRequirement` `ProductPart` `ProductTechRequirement` `TechPrerequisite` `VehiclePathType`（经父实体；父实体可空 → 间接关联亦弱） |

- `CompetitionScopeGuard`（`server/src/common/guards/competition-scope.guard.ts`）全局注册：超管放行；非超管写强制 `body.competitionId = user.competitionId`、读注入 own cid、`cid ≠ own` 抛 Forbidden。
- **绕过口**：`@NoCompetitionScope()` 豁免 5 个控制器——`files`、`contract-type`、`message`、`competition`、`industry-type`；显式传 `competitionId=null` 且账号 own=null 时也放行（读全局字典，设计如此）。

---

## 3. 问题分析（问题清单）

### P1（高）写失效与在途增量竞态 → 「本地存储和增量同步内容冲突」

- 写操作成功后 `invalidateResource` 清掉本地副本并 `_resetMemo`；但**在途的增量请求**（如断线重连对账、其他组件的增量 GET）不受影响，其基于**旧基线**返回的变更可能在失效之后 `patchFullItems` 写回副本，并把基线推进到旧 `serverTime` → 副本混入旧数据、后续增量基于错误基线；
- 两个并发 GET（同一集合，一个全量 `storeAndReturn`、一个增量 `patchFullItems`）先后写副本，后写者以旧底本合并，可能覆盖全量的新副本；
- 结论：**本地副本的写方不互斥、无版本校验**，是"老是有冲突"的核心机制。

### P2（高）断线重连对账不通知组件 → 界面不刷新

- `reconcileAllIncremental`（`resource-changed.ts:83-85` 触发）只更新 IndexedDB 副本与 memo，**不派发 window 事件**，已挂载组件不会重载 → 断线期间他人产生的变更，重连后界面依然旧。

### P3（高）权限变更无实时同步 → 「我加权限对方看不到」

- 无 `permissions:changed` 定向事件；泛化 `resource:changed(users)` 不被 auth store 消费；前端权限状态（`can()`/菜单/按钮）只在登录与 `AppLayout` 挂载时初始化（`AppLayout.vue:40`）。
- 影响面：菜单显隐、按钮 disabled、`can()` 判定全部停留在旧权限；**服务端已按新权限拒绝/放行**，前后端判定不一致。

### P4（高）15s memo 与实时事件竞态 + memo 无账户维度

- `_cachedGet`（`request.ts:551-557`）：窗口内且 `_lastEventAt <= m.time` 直接返回内存旧值。**事件先于 memo 写入**（或事件丢失）时，memo 会在窗口内持续返回旧值；
- memo 键（`_reqKey`）**不含账户维度**：同窗口内切换账号，同 url 同 params 可能命中上一账号的缓存（登出路径是否 `_resetMemo` 需核实）；
- `_lastEventAt` 是全局 Map，不区分比赛：A 比赛的股票事件会使 B 比赛同资源 memo 失效（过度失效，浪费）或相反（漏失效）。

### P5（中）`resource:changed` 载荷缺 competitionId、无序号

- 载荷仅 `{resource, id, action}`（`realtime.service.ts:103`），前端无法按比赛过滤；事件丢失**无序号兜底**，只能靠 5 分钟全量对账或重连对账补齐 → 长在线窗口内容易出现"别人改了看不到"。

### P6（中）股票多集合共用 resource=stocks → 删除事件跨集合误伤

- `MODEL_TO_RESOURCE` 把 `Stock/StockFundsAccount/StockOrder/StockHolding/StockCandle` 全部映射为 `stocks`（`realtime.service.ts:31-34`）；
- 前端 `removeFullItemByResource("stocks", id)` 按前缀 `FULL|stocks|` 扫描**所有股票集合**删除同 id（`cache.ts:202-232`）——不同集合（accounts/orders/holdings）的同 id 是**不同实体**，删除一条订单可能误删一条持仓记录。

### P7（中）22 个模型 `competitionId` 可空 → 弱租户隔离

- 16 个基础数据 + 5 个股票 + Message 均可写入 `null` 形成跨比赛全局数据；`@NoCompetitionScope` 豁免 5 个控制器、显式 null 放行，存在数据越比赛写入/读取面。

### P8（低）界面刷新遗漏点

- 路由复用：`AppLayout.vue:10` `:key="route.path"`，同路由仅 query 变化不重挂载；
- 表单回车：`AccountManagementView.vue:79` `el-form` 缺 `@submit.prevent`（对比 `CompetitionListView.vue:143` 有）→ 回车整页重载；
- 事件漏订阅：`StockMarketView.vue:634` 仅订阅 `company-field`，未订阅 `stocks` → 他人交易/撮合不刷新；
- detail 请求无增量：非列表接口仅按请求键缓存，刷新依赖 memo 过期 + 事件。

### P9（低）对账范围过大

- `reconcileAllIncremental` 遍历**本地全部集合**（含已切换离开的比赛、已失去权限的资源）逐一发增量请求——浪费带宽且可能触发越权请求（虽被服务端拒绝，但产生噪音）。

---

## 4. 目标设计

### 4.1 本地存储按「比赛 + 账户」双层隔离（✅ E1-B 显式命名空间）

**原则**：任何缓存键（IDB / localStorage / memo）必须具备完整身份：`账户 × 比赛`；全局豁免数据（产业类型/合同类型）用显式 `global` 命名空间，与比赛数据物理区分。

| 层 | 现状 | 目标 |
|----|------|------|
| IndexedDB 库名 | `gipfel-client-cache-u<id>` | 保持按账户分库（已隔离），**集合键增加显式 `c<cid>|` 前缀**：`FULL|c<cid>|material|...`；全局豁免资源用 `FULL|global|...` |
| 内存 memo | `GET url params` | 键加入 `uid|cid|` 前缀，登出/切换账号必须 `_resetMemo` |
| localStorage | `acct_u<id>__` 前缀 | 比赛相关键统一 `acct_u<id>__c<cid>__`；全局键 `acct_u<id>__global__` |
| 切换比赛 | 仅 `useCompetitionReload` 清空组件数据 | 切换时：切到目标比赛的命名空间 → 立即对账该比赛 → 广播 `sync:reconciled` |
| 清缓存 | 整库 `clear()` | 支持按比赛粒度清理（删除 `FULL|c<cid>|` 前缀），登出按账户整库清理 |

### 4.2 增量同步防冲突（单一事实源）

1. **写方互斥（generation 标记）**：`request.ts` 维护每集合 `generation`，写操作开始时 `generation++` 并记录；在途增量响应返回时校验 `generation` 未变才允许 `patchFullItems` 写回，否则丢弃（数据随后由失效后的全量同步补齐）；
2. **基线单调推进**：`setBaseline` 仅接受 ≥ 本地基线的时间（ISO 字符串比较），服务端 `serverTime` 为准，禁止回退；
3. **副本版本校验**：`patchFullItems` 写入前记录 `savedAt`，与读底本时校验，若期间被其它写方更新则重读合并（或直接放弃本次 patch，交给全量兜底）；
4. **事件驱动优先，对账兜底**：created/updated 事件到达后组件重拉（增量），deleted 事件精确移除；对账（existingIds）仅用于断线补漏，不再作为常规路径；
5. **集合限定删除**：删除事件携带集合标识（见 4.4），`removeFullItemByResource` 支持按 `_p=` 子集合限定，杜绝 P6 跨集合误伤。

### 4.3 前端刷新机制重构

> 前端侧的详细设计、问题清单（R1–R9）与 F 系列任务见**第二部分『前端刷新机制』**；本节仅列目标要点。

1. **统一事件总线升级**：`resource-changed` 载荷透传 `competitionId` + `seq`；`useResourceChanged` 按 `resource + competitionId` 精确匹配（不匹配当前比赛的事件不触发重载）；
2. **对账完成主动通知**：`reconcileAllIncremental` 完成后派发 `sync:reconciled`（携带涉及的集合），已挂载组件监听后统一重载——直接修复 P2；
3. **组件加载模式统一**：`onMounted 拉取 + 订阅事件重载`，重载统一走 `useResourceChanged`/`useCompetitionReload`，不再各自为政；
4. **路由复用**：`AppLayout.vue` 的 `:key` 改为 `route.fullPath`（含 query），query 变化即重挂载；
5. **表单**：所有 `el-form` 补 `@submit.prevent`（全局检索补齐）；
6. **补订阅**：`StockMarketView` 增加 `stocks` 资源订阅；detail 请求（`/:id`）在所属资源事件到达时一并失效。

### 4.4 实时同步机制重设计（✅ E4-A seq + 补拉）

**统一事件格式**（向后兼容：旧字段保留，新增字段可缺省）：

```
resource:changed → { resource, id, action, competitionId, seq, ts }
```

- **房间路由标准化**：`emitResourceChanged` 改为按实体归属自动路由——有 `competitionId` → `comp-{cid}`；全局豁免实体（IndustryType/ContractType/Competition 等）→ 全体房间；用户定向（权限/消息）→ `user-{id}`；
- **事件序号 seq**：服务端维护单调递增全局序号（内存计数，重启归零可接受），事件与增量响应均携带 `seq`；客户端记录 `lastSeq`，重连后发 `sync:replay {afterSeq}`，服务端补发遗漏事件（内存环形缓冲，保留最近 N 条）；
- **心跳与重连**：现有心跳/重连保留；重连成功 → 先 `sync:replay` 补事件 → 再 `reconcileAllIncremental` 对账 → 最后广播 `sync:reconciled`；
- **多 Tab 一致性**：事件广播与 IndexedDB 写入结合 localStorage `BroadcastChannel`（如适用）同步，保证同账户多窗口一致（Electron 单窗口场景可后置）。

### 4.5 非数据类信息实时主动同步（权限 / 消息 / 公告 / 配置）（✅ E2-A 事件驱动）

| 信息 | 现状 | 目标 |
|------|------|------|
| **权限/角色/范围变更** | 无定向推送，前端不刷新（P3） | 后端 `users.service.update/create` 后 `emitToUser(targetUserId, "permissions:changed", {version})`；**前端 auth store 订阅 → 拉取 `/auth/me` → 更新 `can()`/菜单/按钮**（Pinia 响应式自动生效） |
| 消息 | `message:new` 定向已有 | 载荷补 `competitionId`；读状态变更（已读）实时同步 `message:read` |
| 公告 | `announcementSeenVersion` 本地存储 | 公告变更推送 `announcement:changed`，前端刷新版本号 |
| 比赛配置/财年 | `competition:changed` / `fiscal-year:changed` 已有 | 统一走新事件格式（补 seq） |
| 账户被禁用/顶号 | 401 被动登出 | 定向 `account:disabled` 事件主动登出提示 |

> 关键保证：**权限变更的"数据"（permissions 数组）写库即生效（服务端即时判定），实时推送只解决"前端界面状态同步"**。二者解耦后，即使推送丢失，重连对账也会把 `/auth/me` 拉齐。

### 4.6 数据强制关联比赛（多租户强化）（✅ E3-A 存量归默认比赛）

1. **Schema 必填化**：22 个弱关联模型的 `competitionId Int?` → `competitionId Int`（`Message` 一并必填化，按发送者归属比赛）；
2. **存量迁移**：一次脚本把 `competitionId = null` 的行迁移到「默认比赛」（✅ 决策点 E3-A 已确认），迁移前备份 `dev.db`；迁移后业务查询不再需要容忍 null；
3. **CompetitionScopeGuard 收口**：
   - `@NoCompetitionScope` 白名单收敛为仅全局字典控制器（contract-type / industry-type / competition 的公开读）；
   - 显式 `competitionId=null` 的非豁免写请求一律拒绝；
   - `Message`/`files` 等控制器改为内部按 sender/业务归属收敛；
4. **关联表随父实体**：`PartMaterial` 等 6 个关联表不直接加字段，查询路径经父实体必填 `competitionId` 自然收敛（服务端 findAll 统一按 `where.competitionId` 注入）；
5. **豁免清单固定**：`User`（系统账户，多比赛共用）、`IndustryType`/`IndustryField`（产业类型，全局字典）、`ContractType`（合同类型，全局字典）——写入代码常量 `GLOBAL_ENTITY`，守卫与前端共用。

---

## 5. 端点-事件对照（验收基线）

| 资源 | 事件 | 房间 | 前端处理 |
|------|------|------|---------|
| 16 个基础数据 + 5 个股票 + company-field + contract | `resource:changed` | `comp-{cid}` | 精确匹配 cid → 组件重载（增量） |
| industry-type / contract-type / competition | `resource:changed` | 全体 | 全局命名空间刷新 |
| users | `permissions:changed`（定向） | `user-{id}` | auth store 拉 `/auth/me` 刷新权限状态 |
| users | `resource:changed`（泛化） | 房间 | 仅账号管理列表刷新（保留） |
| messages | `message:new` / `message:read` | `user-{id}` | 消息中心增量 |
| announcements | `announcement:changed` | 全体/房间 | 公告横幅/版本号 |
| 全部 | `sync:reconciled`（客户端本地事件） | — | 已挂载组件统一重载 |

---

## 6. 重构任务清单（代码级，S1–S12）

> 顺序即实施顺序；每步完成后跑对应回归。标注 [文档先行] 的步骤需先更新本文档对应章节。

| # | 任务 | 涉及文件 | 要点 | 验收 |
|---|------|---------|------|------|
| S1 | 事件格式标准化：载荷补 `competitionId`+`seq` | `realtime.service.ts`、`realtime.gateway.ts`、`prisma.service.ts` | 统一 payload；`emitResourceChanged` 按实体归属路由房间 | 事件载荷含 cid/seq；房间路由与实体归属一致 |
| S2 | 前端事件总线升级 | `resource-changed.ts`、`useResourceChanged.ts` | 透传 cid/seq；按 `resource+cid` 精确匹配 | 跨比赛事件不误触发重载 |
| S3 | memo 维度化 + 写互斥 | `request.ts` | `_reqKey` 加 `uid\|cid\|`；generation 校验丢弃过期增量；基线单调推进 | 切换账号/比赛不串数据；写后并发增量不覆盖副本 |
| S4 | 对账完成主动通知 | `request.ts`、`resource-changed.ts`、`useResourceChanged` | `reconcileAllIncremental` 完成派发 `sync:reconciled`；组件监听重载 | 断线重连后界面自动刷新（无需手动） |
| S5 | 对账范围收敛 | `request.ts` | 仅对账「当前比赛 + 已订阅资源」，跳过已离开比赛 | 对账请求数与当前视图一致 |
| S6 | 股票集合精确删除 | `realtime.service.ts`、`cache.ts` | 事件带集合标识（或细分 resource）；`removeFullItemByResource` 支持 `_p=` 限定 | 删除订单不误伤持仓（单测） |
| S7 | 权限实时同步 | `users.service.ts`、`client/stores/auth.ts`、`AppLayout.vue`、`socket.ts` | 后端定向 `permissions:changed`；前端订阅→拉 `/auth/me`→刷新 `can()`/菜单/按钮 | 改权限后对方界面**30 秒内**生效（含重连兜底） |
| S8 | 本地存储比赛隔离 | `cache.ts`、`accountStorage.ts`、`request.ts` | 集合键加 `c<cid>\|` 前缀；切换比赛切命名空间+对账；按比赛粒度清缓存 | 切换比赛无残留；全局/比赛数据物理分离 |
| S9 | 界面刷新遗漏修复 | `AppLayout.vue`、`AccountManagementView.vue`、`StockMarketView.vue`、全局 el-form | 路由 `:key=fullPath`；补 `@submit.prevent`；补 `stocks` 订阅；detail 失效 | 回车不重载；他人交易可见 |
| S10 | 数据强制关联比赛 | `schema.prisma`、迁移脚本、`competition-scope.guard.ts`、各 findAll | 22 模型必填化 + 存量归默认比赛；守卫白名单收敛；`GLOBAL_ENTITY` 常量 | 无法写入 null 比赛数据；存量数据迁移后可读写 |
| S11 | 增量同步防冲突收尾 | `cache.ts`、`request.ts` | patchFullItems 版本校验；删除集合限定；基线单调 | 长时运行无"副本被旧数据覆盖"复现 |
| S12 | 文档同步 | 软件文档、README、本文档 | 写入本文档；同步权限文档的 D 系列联动点 | 文档检索无旧口径残留 |

> 与《权限分级与重构设计.md》联动：S7 依赖权限文档的「账号保存校验授予上限（T4）」先行或并行；权限文档的 T7/T9（前端单一事实源）与本文档 S2/S3 共享 auth store 改造点，实施时合并排期。

---

## 7. 兼容与迁移策略

1. **事件载荷向后兼容**：`resource:changed` 新增字段缺省可解析，旧客户端（未升级前端）不受影响；前端升级后自动启用精确匹配；
2. **本地存储键迁移**：S8 键前缀变更通过「检测旧键 → 读取后删除」一次性迁移（启动时执行一次），失败则回退全量同步；
3. **数据库迁移（S10，一次）**：22 模型 `competitionId` 必填化前，先备份 `dev.db`，跑存量归并脚本（null → 默认比赛），再 `db push`；若后端正在运行需先停止（SQLite 写锁）；
4. **灰度顺序**：S1–S2（事件+总线）→ S3–S4–S5（缓存一致性）→ S6–S9（精确删除+界面修复）→ S7（权限实时，联动权限文档 T4/T7/T9）→ S8（存储隔离）→ S10（数据关联迁移）→ S11–S12（收尾）；
5. **回滚**：S1–S6、S9 集中在 request/cache/realtime 层，可整文件 revert；S7 需前后端同发（客户端 build+重启）；S10 迁移前有备份，异常时还原 `dev.db`。

---

## 8. 决策点（已确认 ✅）

> ✅ **2026-08-17 已确认四项决策**：E1-B 显式命名空间 / E2-A 事件驱动 / E3-A 归默认比赛 / E4-A seq+补拉。本文档 §4.1（E1-B）、§4.5（E2-A）、§4.6（E3-A）、§4.4（E4-A）均已按确认方案编写，重构按第 6 节 S1–S12 实施。

| 决策点 | 选项 | 确认 | 影响 |
|--------|------|------|------|
| E1 本地存储隔离粒度 | A. 保持现有「集合键含 competitionId 参数」+ 补齐 memo/全局命名空间（增量改造）；B. 显式 `c<cid>\|` 命名空间 + 键迁移（彻底） | **✅ B（已确认）** | B 让「比赛」成为一等隔离维度（切换/清理/对账都按比赛为单位），一次性根治；A 成本低但隔离语义分散在 key 参数里 |
| E2 权限变更推送方案 | A. 定向 `permissions:changed` + 前端自动重拉 `/auth/me`（事件驱动）；B. 前端周期轮询 `/auth/me` | **✅ A（已确认）** | A 即时且省流量；B 实现简单但有延迟（轮询间隔内不一致） |
| E3 存量 null 比赛数据归属 | A. 迁移到「默认比赛」（第一个比赛）；B. 迁移到当前操作者所属比赛（逐行）；C. 维持可空但禁止新写 null | **✅ A（已确认）** | A 确定性最高、可脚本化；B 依赖操作者上下文、结果分散；C 治标不治本 |
| E4 事件可靠性 | A. 事件带 seq + 内存环形缓冲 + 重连 `sync:replay` 补拉（低成本）；B. 变更日志表 + 统一 `/api/sync` 增量端点（高成本） | **✅ A（已确认）** | A 覆盖"断线期间事件丢失"主场景，重启服务端归零可接受；B 更强但引入存储与接口复杂度 |

---

## 附录 A：现状关键代码位置索引

| 位置 | 内容 |
|------|------|
| `client/src/api/cache.ts` | IndexedDB 分库/全量副本/基线/增量 patch/失效（S3/S6/S8/S11 改造点） |
| `client/src/api/request.ts` | O3 memo、`cachedGetImpl` 增量、`_mutating` 失效、`reconcileAllIncremental`（S3/S4/S5/S11） |
| `client/src/realtime/resource-changed.ts` | 事件→window 转译、防抖、断线对账触发（S2/S4） |
| `client/src/realtime/socket.ts` | 连接/心跳/订阅/重连（S7 权限事件订阅点） |
| `client/src/realtime/useResourceChanged.ts` | 事件订阅 hook（S2 精确匹配改造点） |
| `client/src/stores/auth.ts` | `can()/canAny()` 权限状态（S7 刷新点，联动权限文档 T9） |
| `client/src/utils/accountStorage.ts` | localStorage 账户前缀（S8 加比赛维度） |
| `client/src/AppLayout.vue` | 路由 `:key`、挂载时拉 `/auth/me`（S7/S9） |
| `client/src/views/stock/StockMarketView.vue` | 仅订阅 company-field（S9 补 stocks） |
| `client/src/views/account-management/AccountManagementView.vue` | el-form 缺 submit.prevent（S9） |
| `server/src/realtime/realtime.service.ts` | 事件格式/房间路由/seq（S1/S6） |
| `server/src/realtime/realtime.gateway.ts` | 握手鉴权/订阅房间/重连（S1） |
| `server/src/prisma/prisma.service.ts` | `$allOperations` 中间件广播（S1 补 cid/seq） |
| `server/src/modules/users/users.service.ts` | update/create 后定向推送权限变更（S7） |
| `server/src/modules/messages/message.service.ts` | `message:new` 补 competitionId（S1） |
| `server/prisma/schema.prisma` | 22 模型 competitionId 必填化（S10） |
| `server/src/common/guards/competition-scope.guard.ts` | 白名单收敛/显式 null 拒绝（S10） |

---

# 第二部分 前端刷新机制（F 系列）

> 原《前端刷新机制重构设计.md》内容，任务编号 F1–F9。本文档是第一部分 §4.3「前端刷新机制重构」的**深化专章**，只讨论「数据如何到达界面、界面如何决定重拉」这一件事；存储/增量/实时通道的整体方案以第一部分为准（任务编号沿用第一部分的 S1–S12，本文档补充 F 系列细化任务）。

## 1. 背景与目标

用户长期反馈「前端界面总是不刷新」：他人改了数据自己的界面不动、断线重连后界面停留在旧状态、管理员加了权限对方看不到。第一部分已从**存储与通道**层面给出方案（S1–S12），本文档聚焦**前端一侧的刷新机制**——组件在什么时机、依据什么信号决定重新拉取，以及数据层如何保证「事件之后的读取必然拿到新数据」。

本次重构目标：

1. **事件驱动成为唯一刷新触发源**：组件重拉只由「匹配的实时事件 / 对账完成通知 / 比赛切换 / 写操作成功」触发，删除组件自实现的各种刷新逻辑；
2. **事件精确匹配**：按 `资源 + 比赛` 双层匹配，跨比赛、全局资源事件不误刷、不漏刷；
3. **消灭 memo 时序竞态**：任何「事件晚于请求发起」的场景都不允许 15s 窗口返回旧值；
4. **断线重连必刷新**：对账完成主动通知组件，无需用户手动刷新；
5. **权限等账号级状态实时生效**：加权限 / 改角色 / 禁用账号后，对方菜单、按钮、`can()` 判定在 30 秒内自动更新。

---

## 2. 现状：刷新链路全景

### 2.1 触发源（4 类）

| # | 触发源 | 实现 | 走向 |
|---|--------|------|------|
| A | 组件主动拉取 | `onMounted` + `useCompetitionReload(reload, clear)`（`useCompetitionReload.ts:17-28`，14 处使用） | 直接调 load 函数 |
| B | 实时事件驱动 | socket `resource:changed` → `resource-changed.ts`（400ms 防抖）→ `window "resource-changed"` → `useResourceChanged` 回调 | 事件 → 组件重拉 |
| C | 写操作成功 | `_mutating` 成功后 `invalidateResource(url)` + `_resetMemo()`（`request.ts:578-586`） | 失效副本+memo → 下次读取全量 |
| D | 周期 / 断线对账 | 5min `FULL_SYNC_INTERVAL_MS`（`request.ts:502`）与断线重连 `reconcileAllIncremental`（`request.ts:601-681`，`resource-changed.ts:83-85`） | 更新副本 → **不通知组件** |

### 2.2 数据层「是否发请求」的判定（memo）

`_cachedGet`（`request.ts:538-564`）：

```
命中条件 = memo 存在 && Date.now() - memo.time < 15s && _lastEventAt[resource] <= memo.time
命中 → 直接返回内存值（不打网络）
未命中 → cachedGetImpl（全量 / 增量 / 离线降级）→ 写回 memo（time = 完成时刻）
```

- `bumpResourceEvent(resource)`（`request.ts:169-172`）在事件到达时把 `_lastEventAt[resource]` 置为当前时间，使该资源 memo 立即失效；
- `_resetMemo()`（`request.ts:175-178`）清空 memo + 事件时间表（写操作 / 登出时调用）；
- `_reqKey`（`request.ts:180-183`）为 `METHOD url params`，**不含账户 / 比赛维度**。

### 2.3 组件接入模式（现状统计）

| 模式 | 数量 | 代表 |
|------|------|------|
| `useResourceChanged(resource, reload)` | 28 处 / 22 文件 | MaterialsManager、PartsManager、ContractManageView 等 |
| `useCompetitionReload(reload, clear)` | 14 处 | 各数据管理页、ContractManageView、MapsManager |
| 自实现防抖 + 单一订阅 | 2 处 | `StockMarketView.vue:627-634`、`StockManageView.vue`（仅订阅 `company-field`，**漏 `stocks`**） |
| 多资源订阅 | 2 处 | `DashboardView.vue:467-470`（4 资源）、`RegionOverviewView.vue:475-480`（4 资源） |

### 2.4 关键缺口（代码事实）

- `useResourceChanged`（`useResourceChanged.ts:12-23`）只按 `detail.resource === resource` 匹配，**不使用事件里的 `competitionId`**（事件 detail 已带 cid，见 `resource-changed.ts:55`）；
- `reconcileAllIncremental`（`request.ts:601-681`）完成后**不派发任何 window 事件**；
- `AppLayout.vue:10` 路由 key 为 `route.path`（同路由 query 变化不重挂载）；`AppLayout.vue:40` 仅挂载时 `fetchProfile()` 一次；
- `AccountManagementView.vue:79` 的 `el-form` 缺 `@submit.prevent`（对比 `CompetitionListView.vue:143` 有）；
- `authStore`（`stores/auth.ts:36-62`）的 `can()/canAny()` 全部基于 `user.value.permissions`，该值只在登录与 `fetchProfile` 时更新，**不消费任何实时事件**。

---

## 3. 问题清单（R 系列）

> R 编号为本文档内部编号；括号内是第一部分的对应 P 编号。

### R1（高）事件不按比赛匹配 → 误刷 / 漏刷（≈P5 前端侧）

- `useResourceChanged` 只看 `resource`：同一资源名（如 `materials`）的事件，无论属于哪个比赛，都会触发当前视图重拉。服务端房间已按比赛隔离（`comp-{cid}`），但全局房间（`industry-types`/`contract-types`/`competitions`）与超管多比赛订阅场景下，**跨比赛事件仍会误触发**；
- 反向缺口：detail 页（如公司详情 `/companies/:id`）若订阅 `companies`，收到列表级事件也会重拉——**粒度是资源级而非实体级**，浪费但无害；
- 后果：多余的网络请求与闪烁；且因 `useResourceChanged` 类型定义（`useResourceChanged.ts:15`）不含 `competitionId`，事件 cid 被丢弃。

### R2（高）memo 时序竞态 → 15s 窗口返回旧值（≈P4）

时序场景（事件到达时已有 in-flight 请求）：

```
t0  组件 A 发起 GET（memo 无缓存）→ 进入 cachedGetImpl（读副本 → 增量请求 in-flight）
t1  事件到达：bumpResourceEvent(resource) → _lastEventAt = t1
t2  in-flight 响应返回：patchFullItems 写回副本，_memo.set(key, {time: t2, value: v_旧})
    —— v_旧 是 t1 之前的数据（不含本次变更）
t3  检查：_lastEventAt(t1) <= memo.time(t2) → 命中 → 15s 窗口内持续返回 v_旧
```

根因：memo 的 `time` 记录的是**完成时刻**而非**数据新鲜点（请求发起时刻）**，事件时间戳与完成时刻比较时被绕过。

### R3（高）断线重连对账不通知组件 → 界面不刷新（=P2）

- `resource-changed.ts:83-85` 在 `connect` 时调 `reconcileAllIncremental`，它只更新 IndexedDB 副本与 memo，**不派发 window 事件**；
- 断线期间他人产生的变更，重连后副本已新、界面依旧旧，直到用户手动切页 / 刷新。

### R4（高）账号级状态（权限/角色/范围/禁用）不实时刷新（=P3 前端侧）

- `AppLayout.vue:40` 挂载时 `fetchProfile()` 一次后不再更新；
- `resource:changed(users)` 只被 `AccountManagementView.vue:442` 消费（刷新账号列表），`authStore` 不感知；
- 后果：菜单显隐、按钮 disabled、`can()` 判定全部停留在旧权限，与「服务端已按新权限放行/拒绝」不一致。

### R5（中）路由复用 → query 变化不重挂载

- `AppLayout.vue:10` `:key="route.path"`：同路由仅 query 变化（如筛选条件放 query 的页面）不重建组件，`onMounted` 不重跑。

### R6（中）表单回车触发原生提交 → 整页重载

- `AccountManagementView.vue:79` 等 `el-form` 缺 `@submit.prevent`；单文本框表单按回车会触发原生提交，路由跳回默认页（表现为「保存后界面重置」）。

### R7（中）股票视图漏订阅 → 他人交易/撮合不可见

- `StockMarketView.vue:634`、`StockManageView.vue:731` 仅订阅 `company-field`；`stocks` 事件（他人下单/成交/轮次推进）不会触发重拉；
- 且组件内自实现 `scheduleAccountReload` 400ms 防抖与全局总线防抖**双重防抖**，模式不统一。

### R8（中）detail 请求无集合副本 → 依赖 memo 过期

- 非列表接口（`/companies/:id` 等 detail）不建集合副本，只按请求键缓存；刷新完全依赖 memo 15s 过期 + 事件。事件到达后 `bumpResourceEvent` 会失效 memo，但 detail 组件若不订阅对应资源事件，则仍不刷新。

### R9（低）对账范围过大（=P9）

- `reconcileAllIncremental` 遍历**本地全部集合**（含已切换离开的比赛、已失去权限的资源），带宽浪费且可能触发无谓的越权请求（被服务端拒绝但产生噪音）。

---

## 4. 目标设计

### 4.1 事件总线精确匹配（useResourceChanged v2）→ 修 R1

**原则**：组件声明「我关心哪类资源、在哪个比赛下」，事件按 `resource + competitionId` 精确匹配后才触发回调。

```ts
// 新签名（向后兼容：options 缺省时按 resource 匹配，行为同现状）
useResourceChanged(resource, onChanged, options?: {
  scope?: "competition" | "global" | "any"; // 默认 "competition"
});
```

匹配规则（以组件当前比赛 `compStore.competitionId` 为基准）：

| 事件 cid | 组件 scope=competition | scope=global | scope=any |
|----------|------------------------|--------------|-----------|
| `=== 当前比赛` | 触发 | 不触发 | 触发 |
| `null`（全局豁免实体） | 不触发 | 触发 | 触发 |
| `≠ 当前比赛` | 不触发 | 不触发 | 触发（按 resource） |

- 存量改造：全局资源组件（`ContractTypeManageView`→`contract-types`、`IndustryTypeManageView`→`industry-types`、`CompetitionListView`→`competitions`）显式传 `scope: "global"`；其余比赛资源组件不传（默认 competition）即可获得精确匹配；
- `useResourceChanged.ts:15` 的事件 detail 类型补 `competitionId?: number | null`；
- 兼容旧服务端：事件 detail 无 `competitionId` 时按 `scope: "any"` 回退，行为同现状。

### 4.2 memo 时序竞态修复 → 修 R2

**核心一行改动**：写 memo 时 `time` 记录**请求发起时刻**而非完成时刻（`request.ts:562`）：

```ts
const startedAt = Date.now();          // 发起前记录
const p = cachedGetImpl(...);           // 原有 in-flight 合并
const result = await p;
_memo.set(key, { time: startedAt, value: result });  // time = 数据新鲜点
```

这样 `_lastEventAt[resource] <= memo.time` 的比较语义变为「事件是否晚于本数据的请求发起时刻」——任何晚于发起的事件都会使该 memo 失效，**从根上消除 R2 的时序窗口**。配套约束：

1. `_getInflight` 合并（`request.ts:548-549`）时，后续等待者拿到的是同一份结果，memo 只写一次（以首个发起者的 `startedAt` 为准）——保持现状即可；
2. `_reqKey` 增加 `uid|cid|` 前缀（沿用第一部分 S3），避免同窗口切账号/比赛串值；
3. `_lastEventAt` 保持全局 Map（事件到达即失效对应资源），不引入按比赛拆分——因为失效是「宁可多失效、不可漏失效」的策略，代价仅是一次额外请求，收益是绝无旧值；
4. 事件到达时**已进入 cachedGetImpl 的请求**仍会正常完成并写回副本（patchFullItems 由第一部分 S3 generation 互斥保护），只是 memo 不再缓存旧结果。

### 4.3 对账完成主动通知 → 修 R3

- `reconcileAllIncremental`（`request.ts:601-681`）在 `finally` 中派发 `window "sync:reconciled"`，detail 携带 `{ collections: string[] }`（本次实际对账的集合键列表）；
- 新增 hook `useSyncReconciled(onReconciled)`（或并入 `useResourceChanged` 内部）：监听 `sync:reconciled`，回调当前视图 `reload()`；
- `resource-changed.ts:83-85` 的 connect 处理不变（触发对账），对账完成由新机制通知组件；
- 防抖：对账可能跨越多个集合，`sync:reconciled` 同样走 400ms 防抖，避免一次重连触发多次组件重拉。

### 4.4 账号级状态实时刷新 → 修 R4

前端先行实现（不依赖后端推送也能降级生效）：

1. `socket.ts` 注册 `permissions:changed` 监听（后端推送方案见第一部分 S7）：收到即调 `authStore.refreshProfile()`；
2. `authStore` 新增 `refreshProfile()`：调 `GET /auth/me` 更新 `user.value`（Pinia 响应式 → `can()/canAny()`、菜单、按钮自动更新），失败静默（离线保持旧状态）；
3. **降级路径**（后端未升级时）：`resource-changed.ts` 收到 `resource=users` 且 `id === 当前用户 id` 的事件时，也调 `refreshProfile()`——写权限的服务端事件必达，确保旧后端下同样生效；
4. `AppLayout.vue:40` 的挂载逻辑保留（首次初始化），权限变更改由事件驱动刷新。

### 4.5 组件加载模式统一（复合 hook）→ 修 R7/R8

**约定**：所有列表 / 详情组件统一采用「刷新三件套」：

```ts
useListReload({
  resources: ["materials"],          // 订阅的资源（比赛资源默认精确匹配当前比赛）
  globalResources: [],               // 全局豁免资源（如 industry-types）
  reload,                            // onMounted + 事件 + 对账完成 统一入口
  clear,                             // 切比赛时清空旧数据引用（可选）
});
```

- 复合 hook 内部组合 `useResourceChanged`（v2）+ `useSyncReconciled` + `useCompetitionReload`，并做一次统一的 400ms 防抖（替代组件内自实现防抖）；
- 存量改造：
  - `StockMarketView.vue:627-634`、`StockManageView.vue:731`：删除组件内 `scheduleAccountReload` 自实现防抖，改用 `useListReload({ resources: ["stocks", "company-field"], reload: reloadAll })`（**补上 `stocks` 订阅**）；
  - 所有现有 `useResourceChanged` + `useCompetitionReload` 组合调用点，机械替换为 `useListReload`（行为等价，改动低风险）；
- detail 页：订阅父资源事件（如公司详情订阅 `companies`），父资源事件到达即重拉——`bumpResourceEvent` 已保证 memo 失效，重拉必走网络。

### 4.6 路由 / 表单 / 订阅补齐 → 修 R5/R6

| 项 | 改动 |
|----|------|
| 路由复用 | `AppLayout.vue:10` `:key="route.path"` → `:key="route.fullPath"`（query 变化即重挂载） |
| 表单回车 | 全局检索 `el-form`，补 `@submit.prevent`（重点：`AccountManagementView.vue:79`） |
| 事件订阅审计 | 每个视图对照「本页展示的资源集合」逐一核对 `useListReload.resources` 是否覆盖（验收项） |

### 4.7 对账范围收敛 → 修 R9

- `reconcileAllIncremental` 增加过滤：仅对账「当前比赛命名空间（第一部分 S8 之后的 `c<cid>|` 前缀）+ 已订阅资源」，跳过已离开比赛 / 无权限集合；
- 第一部分 S5 的验收口径：对账请求数与当前视图订阅一致（超管除外，超管维持全量以便跨比赛切换即见）。

---

## 5. 刷新时序（关键场景）

### 5.1 正常事件刷新（他人修改 → 我的界面更新）

```
服务端写库 → $allOperations → emitResourceChanged {resource,id,action,cid,seq}
  → comp-{cid} 房间 → socket → resource-changed.ts
  → bumpResourceEvent(resource)          // memo 立即失效（R2 修复后语义正确）
  → 400ms 防抖 → window resource-changed
  → useListReload 匹配 resource + cid = 当前比赛 → reload()
  → cachedApi.get → memo 未命中（事件已失效）→ 增量请求 → 新数据渲染
```

### 5.2 断线重连

```
reconnect → resource-changed.ts connect 处理 → reconcileAllIncremental()
  → 逐集合增量 + existingIds 复核 → 更新副本 + 基线
  → finally：400ms 防抖 → window sync:reconciled
  → useListReload 回调 reload() → 界面自动刷新（R3 修复后不再需要手动）
```

### 5.3 写操作后立即读取

```
_mutating 成功 → invalidateResource + _resetMemo
  → 同一组件 reload() → memo 空 → 全量同步（首次）或增量（有副本时重建）
  → 在途增量响应（旧基线）由第一部分 S3 generation 互斥丢弃 → 副本一致
```

### 5.4 权限变更

```
（新后端）users.service 改权限 → emitToUser(uid, "permissions:changed")
（旧后端降级）resource:changed {resource:"users", id: 我}
  → authStore.refreshProfile() → GET /auth/me → user.value 更新
  → can()/菜单/按钮 响应式刷新（30 秒内）
```

---

## 6. 任务清单（F 系列细化，承接第一部分 S2/S3/S4/S5/S7/S9）

> 顺序即实施顺序；与第一部分任务合并排期（括号内为对应 S 任务）。

| # | 任务 | 涉及文件 | 要点 | 验收 |
|---|------|---------|------|------|
| F1 | 事件总线精确匹配（useResourceChanged v2） | `useResourceChanged.ts` | detail 类型补 cid；`scope` 选项；按 `resource+cid` 匹配；旧载荷回退 | 跨比赛/全局事件不误刷当前视图（单测） |
| F2 | memo 时序竞态修复 | `request.ts` | 写 memo 用 `startedAt`；`_reqKey` 加 `uid\|cid\|`（随 S3） | 事件晚于请求发起时 15s 窗口内不返回旧值（时序单测） |
| F3 | 对账完成主动通知 | `request.ts`、`resource-changed.ts`、新 `useSyncReconciled.ts` | reconcile 完成后派发 `sync:reconciled`（400ms 防抖） | 断线重连后界面自动刷新，无需手动 |
| F4 | 账号级状态实时刷新 | `socket.ts`、`stores/auth.ts`、`resource-changed.ts` | 订阅 `permissions:changed` + `users` 事件降级；`refreshProfile()` | 改权限后 30s 内 can()/菜单/按钮更新 |
| F5 | 复合 hook useListReload | 新 `useListReload.ts` | 组合三 hook + 统一防抖；存量调用点机械替换 | 列表组件统一三件套，无自实现刷新 |
| F6 | 股票视图补订阅 | `StockMarketView.vue`、`StockManageView.vue` | 删自实现防抖；补 `stocks` 订阅 | 他人交易/撮合/轮次推进可见 |
| F7 | 路由 / 表单修复 | `AppLayout.vue`、全局 el-form | `:key=fullPath`；补 `@submit.prevent` | 同路由 query 变化重挂载；回车不整页重载 |
| F8 | 对账范围收敛 | `request.ts` | 仅对账当前比赛 + 已订阅资源 | 对账请求数与视图一致（超管除外） |
| F9 | 订阅审计 + 文档同步 | 各视图、本文档、软件文档 | 每视图核对 resources 覆盖；写回本文档 | 无「页面展示 X 但未订阅 X」残留 |

> 与第一部分衔接：F1/F3/F8 分别承接 S2/S4/S5；F2 承接 S3 的 memo 维度化；F4 承接 S7 的前端侧（后端 `permissions:changed` 推送见 S7 服务端部分）；F5/F6/F7 承接 S9。实施时按第一部分 §7 灰度顺序排期，本文档只改文档、不单独排代码日程。

---

## 7. 兼容与迁移策略

1. **事件总线向后兼容**：`useResourceChanged` 旧调用（不传 options）行为 = `scope:"any"` 回退，等价现状——F1 可全量替换后逐页验收；
2. **memo 改动无迁移成本**：`startedAt` 语义只在运行时生效，无持久化数据；
3. **对账通知新增事件**：`sync:reconciled` 是纯增量事件，旧组件不监听则无感知；
4. **账号刷新降级路径**：F4 不阻塞于后端 S7 是否上线，旧后端靠 `users` 事件降级生效；
5. **回滚**：F1–F3、F5–F8 全部集中在 `client/src` 前端文件，可逐文件 revert；F4 需前后端同发（前端 build + 重启），回滚时两端同步还原。

---

## 8. 验收基线（用户体验口径）

| 场景 | 现状 | 目标 |
|------|------|------|
| 他人修改同一比赛数据 | 依赖组件订阅覆盖，部分页面不刷新 | 展示该数据的页面 1s 内自动更新 |
| 他人修改其它比赛数据 | 可能误刷当前视图 | 当前视图完全不动 |
| 断线 30s 后重连 | 界面停留在旧数据，需手动刷新 | 自动对账并刷新 |
| 管理员给我加/减权限 | 菜单按钮不更新，直到重启 | 30 秒内自动生效 |
| 同一页面仅 query 变化 | 不重挂载，可能显示旧内容 | 重挂载并拉取 |
| 表单回车 | 整页重载 | 不触发提交 |

---

## 附录 B：前端刷新现状代码位置索引

| 位置 | 内容 |
|------|------|
| `client/src/realtime/useResourceChanged.ts` | 事件订阅 hook（F1 改造点，缺 cid 匹配） |
| `client/src/realtime/resource-changed.ts` | 事件转译 / 400ms 防抖 / connect 触发对账（F3 派发点） |
| `client/src/realtime/socket.ts` | socket 连接 / 重连 / 事件注册（F4 权限事件订阅点） |
| `client/src/api/request.ts` | `_cachedGet` memo 判定（L538-564）、`_mutating` 失效（L566-588）、`reconcileAllIncremental`（L601-681）、`bumpResourceEvent`（L169-172） |
| `client/src/api/cache.ts` | IndexedDB 集合键 / patchFullItems / removeFullItemByResource |
| `client/src/stores/auth.ts` | `user.value.permissions`、`can/canAny`（L36-62）、`fetchProfile`（L94） |
| `client/src/composables/useCompetitionReload.ts` | 切比赛重拉 hook（F5 组合进 useListReload） |
| `client/src/components/layout/AppLayout.vue` | 路由 `:key="route.path"`（L10）、挂载时 fetchProfile（L40） |
| `client/src/views/stocks/StockMarketView.vue` | 自实现防抖 + 仅订阅 company-field（L627-634） |
| `client/src/views/stocks/StockManageView.vue` | 同上（L731） |
| `client/src/views/account-management/AccountManagementView.vue` | el-form 缺 @submit.prevent（L79）、users 事件仅刷列表（L442） |
| `client/src/views/data-management/ContractTypeManageView.vue` 等 | 全局资源组件（F1 需标 scope:"global"） |
